# Copy to keystore.properties and replace every placeholder.
# Keep keystore.properties and the private keystore out of version control.
storeFile=release.keystore
storePassword=CHANGE_ME
keyAlias=degoogle-release
keyPassword=CHANGE_ME
